import { CanvasParameters } from './canvas-parameters.model';

describe('CanvasParameters.Model', () => {
  it('should create an instance', () => {
    expect(new CanvasParameters()).toBeTruthy();
  });
});
